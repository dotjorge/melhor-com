import styled from 'styled-components'

const InputContainer = styled.div`
  position: relative;
`

const CalendarIcon = styled.div`
  position: absolute;
  bottom: 50px;
  right: 20px;
`

export default { InputContainer, CalendarIcon }
